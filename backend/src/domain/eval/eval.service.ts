import { Injectable } from '@nestjs/common';

/**
 * Self-improvement loop: scores inqi's own runs (SelfEval) and proposes workflow
 * improvements published as new versioned WorkflowDefinitions. Stub for now.
 */
@Injectable()
export class EvalService {
  // TODO: score a completed run and persist a SelfEval; propose a new workflow version.
}
